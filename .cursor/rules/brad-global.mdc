---
description: Brad global discipline — coding, communication, secrets, cross-machine. Mirrors ~/.claude/rules/.
alwaysApply: true
---

<!-- GENERATED by AI-Crew/scripts/sync/sync-ide-config.sh from ~/.claude/rules/.
     Do not edit here — edit the source rule files and re-run "ide sync". -->

# coding-discipline

# Coding Discipline

Imperative rules for code edits. Active in every session via `~/.claude/CLAUDE.md`.

Derived from [multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills) (MIT). For diff-level bad/good examples, see [EXAMPLES.md upstream](https://github.com/multica-ai/andrej-karpathy-skills/blob/main/EXAMPLES.md).

## 1. Think Before Coding

Don't assume. Don't hide confusion. Surface tradeoffs.

- State assumptions explicitly. If uncertain, ask.
- Multiple interpretations exist → present them. Don't pick silently.
- Simpler approach exists → say so. Push back when warranted.
- Something unclear → stop. Name what's confusing. Ask.

## 2. Simplicity First

Minimum code that solves the problem. Nothing speculative.

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- 200 lines and could be 50 → rewrite.

Test: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

Touch only what you must. Clean up only your own mess.

When editing existing code:

- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- **Match existing style, even if you'd do it differently.**
- Notice unrelated dead code → mention it, don't delete it.

When your changes create orphans:

- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

**The test: every changed line should trace directly to the user's request.**

## 4. Goal-Driven Execution

Define success criteria. Loop until verified.

Transform tasks into verifiable goals:

- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "**Write a test that reproduces it, then make it pass.**"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:

```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**Tradeoff:** These rules bias toward caution over speed. For trivial tasks, use judgment.

**These rules are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, clarifying questions come before implementation rather than after mistakes.


# communication-style

# Communication Style

## Internal (Day-to-Day with Brad)

- Clear, concise, and professional
- Plain language — direct messaging, no filler
- Efficient: get to the point, then provide supporting context
- No casual or informal language that undermines professionalism
- No emojis
- No AI attribution, no "great question!", no sycophantic openers
- Mix of formats as appropriate: bullets for lists, short paragraphs for explanation, tables for comparisons

## Writing Principle

Simple and clear with enough context to be meaningful. Not terse to the point of losing nuance — but never padded.

## External / Public-Facing (Byte & Barrel, YouTube, client-facing)

- Friendly, authoritative, and approachable
- Brand voice: engaging and trust-building
- Polished — no rough edges in anything that goes out
- Audience-aware: adjust formality to the context (e.g., more formal for finance/enterprise, more casual for lifestyle/consumer content)
- Consistent and customer-centric

## Formatting Preferences

- Mix bullets, paragraphs, and tables based on what communicates best
- Use headers to organize anything longer than a few sections
- No wall-of-text responses — break up dense content
- When referencing code or file paths, use markdown links where clickable in VS Code
- **Open items / review asks MUST surface the artifact (standing rule, all projects, both machines — Brad, 2026-07-21).** Any item Brad is asked to review, validate, decide, or act on must put the exact artifact in front of him:
  - **Local files: markdown link with a path RELATIVE to the workspace root** — `[label](relative/path/from/workspace/root)`, e.g. `[video_ids.json](plainspoke/out/video_ids.json)`. This is the global standard link format; it opens in the center panel. (Verified working 2026-07-21 after Brad's editor update; links had regressed earlier that day — if they ever die again, fall back to opening via `code <path>` and flag the regression.)
  - **Visual yay/nay decisions (thumbnails, mockups, design picks): additionally OPEN the image(s) via `code <path>`** at the moment of asking so the asset is on screen — per the decision-with-asset rule.
  - **Browser resources (YouTube Studio, dashboards, external sites): URL links.**
  - Never hand Brad an action item he has to go hunt for.


# secrets-handling

# Secrets Handling

## Source of Truth

macOS Keychain via `keyring` (Python) or `security` (CLI). Never disk, never
repo, never a committed `.env`, never chat output.

Registry: [`~/.claude/keychain-index.json`](../keychain-index.json) — a
pointer-only catalog, no values. **Update it on every Keychain write**, or future
sessions won't know the key exists.

## 1. Never echo a secret value

Not in chat, not in CLI arguments (visible in `ps`, shell history, and tool
logs), not in commit messages or diffs, not in screenshots, not in plan files.

To prove a slot is populated, show a short prefix only:

```bash
security find-generic-password -s "<service>" -a "<account>" -w | head -c 12
```

## 2. Write it yourself — don't make Brad retype it

When a new secret appears, create the Keychain entry for him.

```bash
# BAD — value in argv leaks to ps, shell history, and the Bash tool log
security add-generic-password -s "openrouter" -a "api_key" -w "sk-or-v1-d79..."

# Good — value comes from env, never argv
security add-generic-password -s "<service>" -a "<account>" -w "$SOME_ENV_VAR" -U

# Good — interactive prompt, nothing echoed
security add-generic-password -s "<service>" -a "<account>" -U

# Good — Brad pasted it into chat; take it via secure stdin
read -s SECRET && security add-generic-password -s "<svc>" -a "<acct>" -w "$SECRET" -U && unset SECRET
```

## 3. Naming

`service` = project-or-vendor slug (`account-agent`, `slated`, `openrouter`);
`account` = `<vendor>_api_key` or `api_key`. In use: `slated/anthropic_api_key`,
`account-agent/anthropic_api_key`, `openrouter/api_key`,
`account-agent/serper_api_key`.

## 4. Index every write

`account-agent` syncs automatically — the module now lives in bauto
([`bauto/keychain_index.py`](../../Projects/automation-lib/bauto/keychain_index.py)),
consumed via `lib/credentials.py`. Manual writes from a session must append an
entry themselves:

```json
"VENDOR_SCOPE": {
  "name": "Human-readable label",
  "note": "What this is for. Keychain slot: service=<svc>, account=<acct>.",
  "type": "api_key",
  "vault": "Personal" | "account-agent" | "<scope>"
}
```

Pointer only — never a value, in the note or anywhere else.

## 5. Exposed secrets — secure in place, do NOT rotate

**Brad's standing decision (2026-06-07): do not rotate keys, and do not surface
rotation as a recommendation.** Previously-exposed keys are secured in place
(moved to Keychain, plaintext removed) and the residual risk is knowingly
accepted — a deliberate choice, made after seeing the OWASP/GitHub
rotate-first guidance. This supersedes the older "flag for rotation" practice
and its 2026-05-17 rotate-list.

So: never write "rotate" / ROTATE flags into docs, the index, or memory. Raise
rotation **only on an actual problem** — anomalous or unauthorized usage at the
provider, a *new* exposure, or a secret-scanning alert.

Everything else in the hygiene stack still applies: don't paper over a leak
(clean up plaintext and history when found), no plaintext at rest, and the
`gitleaks` pre-commit guard (template at `~/.claude/templates/gitleaks-pre-commit`).
Accepted-risk register with revisit triggers:
[`~/.claude/SECRETS_REGISTER.md`](../SECRETS_REGISTER.md); the index marks those
keys `SECURED (no rotation, accepted)`.

## 6. ProtonPass for human logins

Login + password combos (account sign-ins, SaaS dashboards) go in ProtonPass.
Keychain is for programmatic API tokens consumed by code.

`account-agent`'s `secure_cleanup()`
([`lib/keystore.py`](../../Projects/account-agent/lib/keystore.py)) `rm -P`
shreds ephemeral artifacts (`output.env`, `protonpass-import.csv`) after use.

## 7. Payment cards — never automated

`account-agent` hard-rejects card entry from automation. Brad enters cards
himself at the service's UI; never typed by an LLM, never stored in a local
artifact. See
[`lib/handoff.py`](../../Projects/account-agent/lib/handoff.py)
`HANDOFF_MESSAGES["payment_entry"]`.

## Related

- [never-echo-secrets](../projects/_shared/memory/feedback_never_echo_secrets.md) — origin story (2026-05-15 OpenRouter incident)
- [no-rotation policy](../projects/_shared/memory/feedback_secrets_no_rotation_policy.md) — the §5 decision in full
- [account-agent state](../projects/_shared/memory/project_account_agent.md)


# cross-machine

# Cross-Machine Workflow — Mini ↔ Main

Two Macs on one Tailscale tailnet (`tailf54584.ts.net`):

| Role | Machine | Tailscale name | User | Projects path |
|------|---------|----------------|------|---------------|
| **main** | MacBook Air | `brads-macbook-air` | `bflanary` | `/Users/bflanary/Projects/` |
| **mini** | Mac mini | `brads-mac-mini` | `bflanary_mini` | `/Users/bflanary_mini/Projects/` |

`ssh main` / `ssh mini` work via Tailscale SSH (aliases in `~/.ssh/config`, no keys).

**Tailscale must be the open-source CLI build** (`brew install tailscale`) — the
sandboxed App Store / `macsys` build does not run the SSH server. If that
sysext is ever re-enabled it steals `/var/run/tailscaled.socket` and SSH breaks
silently: disable it in System Settings → General → Login Items & Extensions →
Network Extensions, `kill` the sysext daemon, `sudo brew services restart tailscale`.

If `ssh` fails: `tailscale status` (peer offline?), then
`tailscale debug prefs | grep RunSSH` on the far machine (must be `true`).
Re-auth: `sudo /opt/homebrew/opt/tailscale/bin/tailscale up --ssh --accept-routes`.
Brew service `homebrew.mxcl.tailscale`; logs `/opt/homebrew/var/log/tailscaled.log`.

## Canonical Machine Per Project

Code is git-synced everywhere — **develop on either machine**. "Canonical" governs
where *production launchd jobs and non-git runtime state* live. Drive the
canonical machine remotely with `ssh <machine>` + tmux.

| Project | Canonical | Notes |
|---------|-----------|-------|
| youtube-automation | **mini** | Since 2026-07-21. All FF/MA50/Plainspoke pipelines, watchdog, OAuth pollers, playlist rotation. |
| plainspoke | **mini** | Engine + research launchd + OAuth + uploads. |
| helm | **mini** | Podman stack + iMessage bridge + Ollama, always-on. See [[project_helm]]. |
| gnav | **mini** | Runtime + master chart archive (2 TB drive); air accesses over Tailscale. See [[project_gnav]]. |
| voyage_planning | **mini** | Weekly fuel job. |
| brad-auto | **mini** | Inbox-calendar-sync. |
| homelab | **mini** (proto) → future NAS | |
| account-agent | **main** | Playwright + ProtonMail Bridge live here. |
| yacht_search | **main** — migration pending | Should be mini (boat), but `com.ops.yacht-search` has four main-only deps: ProtonVPN WireGuard tunnel, the Lima fetch VM `com.ops.fetchvm`, a Python venv, and Scrape.do/Scrapfly keys in main's Keychain. Migration is a port, not a flag flip. |
| ops-dashboard | both | Read-only on either. |
| contract-analysis | **main** (Air) | Declared 2026-08-23. Runs from `~/Projects/contract_comparison/backend` on **:8001**, labels `com.bflanary.contract-analysis.{backend,frontend}`. mini has a second live deployment of the *same repo* (`~/Projects/contract-analysis`, **:8000**, labels `com.contractanalysis.*`) — park it rather than leaving two. |
| slated | either | Deploy remote; declare active machine in `session_last.md`. |
| AI-Crew · printing-press | either | Pure code, no per-machine state. |

**Boat projects are mini-canonical** (Brad, 2026-06-25): gnav, helm,
voyage_planning, opencpn.

**yacht_search is the one open exception.** The intent is mini — it is a boat
project — but the runtime is on **main** and stays there until the port lands;
the table row above lists the four main-only dependencies that make it a port
rather than a flag flip. Until then, main is where it runs and where it is
fixed. Stating the aspiration as fact is what previously made this section
contradict its own table, and a rule that contradicts itself gets read
selectively — the reader keeps whichever half suits them.

### The rule that matters

**Never run the same scheduled job on both machines.** Duplicates have caused
real damage twice: duplicated `voyage_planning` fuel jobs produced same-week
double commits that collided on push (2026-06-25), and duplicate YT pipelines
produce duplicate playlist inserts because `~/.yt_playlist_rotation.json` is
mini-local and unsynced.

main's disabled production plists are parked in
`~/Library/LaunchAgents/disabled-canonical-mini/`. **Do not re-enable them.**
Remotion `com.ops.plainspoke-pipeline` is permanently disabled (off-brand).

To run a YT pipeline "from" main: `ssh mini` and trigger it there.

When unclear which machine did the work, check the newest `session_last.md` in
`~/.claude/projects/-Users-bflanary*/memory/`.

## Switching Machines

1. Wrap up: commit + push, write `session_last.md`.
2. On the other machine: `git pull` the repo(s) **and** `~/.claude`.
3. Non-git state needs rsync (below).

## Ad-hoc State Sync

Runtime state (gcloud creds, YT tokens, metrics.db, work_dirs) is not in git and
never auto-syncs.

```bash
rsync -av mini:~/.config/gcloud/ ~/.config/gcloud/          # after a fresh gcloud auth login
rsync -av mini:~/.yt_tokens/ ~/.yt_tokens/                  # mini = source of truth
rsync -av mini:~/.ops_metrics/metrics.db ~/.ops_metrics/metrics_mini.db   # read-only mirror
rsync -av mini:~/Projects/youtube-automation/work/<channel>/<run_id>/ /tmp/inspect-<run_id>/
```

## Sudo

Narrow NOPASSWD entry per machine at `/etc/sudoers.d/<user>`: tailscale CLI,
`brew services`, `kill`, `rm`, `cp`, `mv`, `launchctl`, `systemextensionsctl`.
Anything else prompts. Edit with `sudo visudo -f /etc/sudoers.d/<user>`;
validate with `visudo -c`.


# diagrams

# Diagrams / Drawings

When asked for a diagram, drawing, architecture diagram, flowchart, system map, or "draw me ...":

**Standard: modern, dark-mode, exported to PDF (+ PNG for inline preview). NEVER use mermaid — Brad considers it ugly/not modern.**

## Tool choice
- **Default → D2** (`d2`): text → polished dark diagram, native PDF+PNG.
- **Icon-rich cloud/infra → mingrammer `diagrams`** (Python; interpreter `~/.diagram-tools/venv/bin/python`, needs Graphviz `dot`).
- **Fully bespoke visual → hand-authored SVG → `rsvg-convert`**.

## Output convention
Write into the project's `diagrams/` folder. Produce `<name>.pdf` (primary, dark) + `<name>.png` (inline preview) + keep the source (`.d2`/`.py`/`.svg`). In markdown: embed PNG, link PDF.

## Commands
```bash
# D2 (default) — dark themes: 200 Dark Mauve, 201 Dark Flagship Terrastruct
d2 --theme 200 --pad 40 diagrams/<name>.d2 diagrams/<name>.pdf
d2 --theme 200 --pad 40 diagrams/<name>.d2 diagrams/<name>.png

# mingrammer diagrams — set graph_attr bgcolor "#0b0f17" + light fontcolor for dark mode
~/.diagram-tools/venv/bin/python diagrams/<name>.py   # Diagram(..., outformat=["pdf","png"])

# bespoke SVG → PDF/PNG
rsvg-convert -f pdf -o diagrams/<name>.pdf diagrams/<name>.svg
rsvg-convert -f png -z 2 -o diagrams/<name>.png diagrams/<name>.svg
```

Claude Code: a richer `/diagram` skill exists at `~/.claude/skills/diagram/`. Always read the rendered PNG back to self-check (overlaps, legibility, dark mode) before reporting done.


# prompt-caching

# Prompt Caching — Mandatory for All Anthropic API Code

Anthropic flagged the StaveandSpirit org for a low cache hit rate (~33% potential API-spend savings, 2026-07-13). Every session that writes or touches Anthropic API code applies these rules. Source: https://platform.claude.com/docs/en/build-with-claude/prompt-caching

## Core rule

Any `messages.create` / `messages.stream` call that reuses a system prompt, tool list, or document context across calls MUST set a cache breakpoint. No exceptions for new code; existing code gets it whenever the file is being touched anyway (surgical-change rule still applies — don't bulk-sweep stable repos unprompted).

## How (Python — the default on this machine)

Use the bauto helpers, not hand-rolled `cache_control` dicts:

```python
from bauto import anthropic_client
client = anthropic_client.make_client("<service>", "anthropic_api_key")
resp = client.messages.create(
    model="claude-opus-4-8",
    system=anthropic_client.cached_system(BIG_STABLE_PROMPT),   # breakpoint on last block
    tools=anthropic_client.cache_tools(MY_TOOLS),               # caches tools prefix too
    messages=[...],
)
```

- Loops/daemons where reuse spans >5 min (launchd jobs, Helm, pipelines): pass `ttl="1h"` — the default 5-minute cache silently expires mid-loop and you pay full price again. 1h writes cost 2× (vs 1.25× for 5m), so it needs ≥3 reads to pay off.
- Non-bauto projects (Node, raw HTTP): put `"cache_control": {"type": "ephemeral"}` on the last block of the stable prefix (`system` blocks array or last tool definition).

## Design rules (these matter more than the marker)

Caching is a **prefix match** — one changed byte invalidates everything after it. Render order: `tools` → `system` → `messages`.

1. **Freeze the system prompt.** Never interpolate `datetime.now()`, run IDs, UUIDs, user names, or mode flags into `system`. Dynamic context goes into the user message, after the breakpoint.
2. **Deterministic serialization.** `json.dumps(..., sort_keys=True)`; never iterate a `set` into the prompt. Sort tool lists by name; don't vary the tool set per request.
3. **Stable-first ordering.** never-changes → per-session → per-turn → per-request, in that order. Breakpoint at the stability boundary, not the end of the whole prompt.
4. **Multi-turn agent loops:** also mark the last content block of the newest turn so the conversation prefix accrues incrementally.
5. **Don't cache one-shot calls** or prompts that differ from byte 0 — it only pays the write premium.
6. **Minimum cacheable prefix** 512–4096 tokens depending on model (exact per-model values below — they are *not* monotonic across generations) — short prompts silently don't cache (no error, `cache_creation_input_tokens: 0`).

## Verify (do this after adding caching)

Check `resp.usage`: `cache_read_input_tokens > 0` on the second identical-prefix call = working. Zero across repeated calls = a silent invalidator (timestamp/UUID/unsorted JSON/varying tools) — diff the rendered prompt bytes between two requests. `bauto.pricing` / `ops_metrics.log_anthropic_response` already record the cache token classes, so the ops-dashboard Cost & Usage view shows the hit rate.

## Sweep status (2026-07-13, mini) — the part worth remembering

Wired already: helm router, YT `shot_list_gen` + `script_gen`, contract-analysis,
receipts-vault. The code carries the markers — don't re-audit *those files* on a whim.

**But that is not a licence to stop looking.** A point-in-time sweep decays: spend
composition drifts, new consumers appear that the sweep never saw, and a model swap can
silently drop a prompt below its cacheable minimum. That is exactly what happened between
2026-07-13 and 2026-08-25 — brad-kb did not exist at sweep time and became 63% of direct
API spend, and Anthropic's mailing list ended up being the monitor.

**Standing verification (do this instead of re-auditing files):** the check is on *new or
changed (api_key, model) pairs*, not on already-swept code. A pair with >1M uncached
tokens/30d and zero cache reads for 7+ days is the signal. Full audit and monitor design:
[`ops-docs/audits/anthropic-cache-efficiency-2026-08-25.md`](../../Projects/ops-docs/audits/anthropic-cache-efficiency-2026-08-25.md).

**A 0% hit rate is often correct** — a once-daily job (cache TTL maxes at 1h), a one-shot
call, or a prompt under the model's minimum prefix. Never "fix" one without checking
`usage.cache_read_input_tokens` first. Minimums are **not monotonic** across generations:
512 (Opus 5), 1024 (Sonnet 5 / Sonnet 4.6 / Opus 4.8), 2048 (Opus 4.7), 4096 (Haiku 4.5,
Opus 4.6). Dropping below is a silent no-op, never an error.

**Caching is not the only lever, and often not the biggest.** It prices input only — on
the workload audited 2026-08-25, output was 53% of spend. Check model choice and the Batch
API (50%, and it halves output too) before reaching for `cache_control`.

**Evaluated and deliberately SKIPPED — do not re-attempt unless the prompt shape
changes:** `helm/app/llm/vision.py`, `plainspoke*/llm.py` + `research/*`,
`CSMYS_Automation/src/parser.py`, `HotTub/backend/vision.py`, and the
once-per-run YT stages (visual_qa, thumbnail_judge, comment_responder,
script_validator). All are one-shot and/or below the model's minimum cacheable
prefix, so a marker is a silent no-op or pure write premium.

Open idea, not wired: YT stages all embed the same ~8K-token script in different
prompts. Putting the script FIRST and instructions after would make it a shared
cacheable prefix across stages within a run. Invasive — revisit only if YT input
spend becomes a concern.


